// eslint-disable-next-line import/prefer-default-export
export enum EModules {
  Logs = 'logs',
  Users = 'users',
  Party = 'party',
  Profiles = 'profiles',
  Inventory = 'inventory',
  BugReport = 'bugReport',
}
